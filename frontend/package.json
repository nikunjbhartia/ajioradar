{
  "name": "ajio-deal-matrix-frontend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && python3 ../backend/export_cloudflare.py",
    "preview": "vite preview",
    "deploy:pages": "npx wrangler pages deploy dist --project-name ajio-flash-deals"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
